# Managed by LINDS-Ansible: roles/router/files/etc/systemd/system/apt-daily-upgrade.timer.d/router.conf
# Debian's default is 06:00 +0-60 min. Run the unattended pass in the
# quietest hour instead; daemon restarts from needrestart land here too.
[Timer]
OnCalendar=
OnCalendar=*-*-* 04:00
RandomizedDelaySec=30m
